export const BASE_URL = 'http://localhost:5000'
export const STORAGE_TOKEN_KEY = 'accessToken'

export enum Routes {
    home = '/',
    contacts = '/contacts',
    login = '/login',
    register = '/register',
    notFound = "*",
}